# 2026-03-24 — Daily Log

## WorkAdventure Setup (village.getdealflow.co)

### Map Rebuilding
- Rebuilt `village.tmj` (50x40 tiles) from scratch — original had only 5 tile IDs (visually empty)
- Final map has 75 unique tiles: walls, floors, furniture, chairs, plants, bookshelves, presentation screens
- Three main zones: **Sales Office** (left), **Dev Center** (center), **Ops & Agents** (right)
- Lobby area with trees, benches below
- Agent desks: Bumblebee (37,5), McFly (37,11), Main (37,17), Closer (44,5), Liquidador (44,11)
- Jitsi rooms configured for Sales, Dev, and each agent zone
- Area names via `roomName` property: 🏢 Sales Office, 💻 Dev Center, 🤖 Ops & Agents, 🐝 Bumblebee's Hive, ⚡ McFly's Desk, 👾 Main Agent Station, 🌳 Lobby
- Build/upload flow: `npm run buildmap` → `npx upload-wa-map -u http://localhost:8087/map-storage -k $SECRET_KEY -d village -v`

### Matrix (Continuwuity) Integration
- Added Continuwuity (Matrix homeserver) to docker-compose at `matrix-continuwuity:6167`
- Image: `forgejo.ellis.link/continuwuation/continuwuity:latest`
- Server name: `matrix.getdealflow.co`
- Created admin user: `@felipe:matrix.getdealflow.co` via registration token `tmCdeOklL7TKAI5U`
- Password in `.env`: `MATRIX_ADMIN_PASSWORD=4vh1CJql8ZbCheAPR2PWRKNH`
- Fixed `MATRIX_API_URI` — needed trailing slash: `http://matrix-continuwuity:6167/` (WA concatenates `_matrix/...` directly)
- Nginx updated with `/_matrix/` proxy and `.well-known/matrix/{server,client}` endpoints

### URL Format Fix
- WA without Admin API does NOT support `/@/` URLs — returns "Unsupported URL format"
- Correct format: `/_/global/village.getdealflow.co/map-storage/village/village.tmj`
- `START_ROOM_URL` updated in `.env`

### Agent Bots (AgentAdventure)
- Built custom bot runner at `/home/felipe/workadventure/bots/agent-bot.mjs`
- Uses Playwright + headless Chromium to log into WA as anonymous users
- Flow: navigate → enter name → click continue → select woka → click continue → wait for canvas → WA.player.moveTo()
- Idle movement: random offset within 3 tiles every 15-30 seconds
- All 5 agents launched and LIVE: Bumblebee, McFly, Main, Closer, Liquidador
- Memory usage: ~100MB per bot, total server at 5.7GB/7.5GB
- Bots launched with `nohup`, logs at `/tmp/bot-{name}.log`
- Found AgentAdventure repo (github.com/droxey/agentadventure) but it's plan-only, no implementation

### Key Config Files
- `/home/felipe/workadventure/.env` — all WA config
- `/home/felipe/workadventure/docker-compose.yaml` — services including matrix-continuwuity
- `/home/felipe/workadventure/nginx.conf` — reverse proxy with Matrix routes
- `/home/felipe/workadventure/map/village.tmj` — the map
- `/home/felipe/workadventure/bots/agent-bot.mjs` — bot runner

### Active Agents on Server
- 🐝 Bumblebee (`autofix`) — Opus — code quality guardian
- ⚡ McFly (`mcfly`) — Opus — CI/CD, LinkedIn crons
- 👾 Main (`main`) — Opus — main agent, crons (CI poll, quality scan, daily summary)
- 🤝 Closer (`closer`) — Sonnet — LinkedIn hooks, sales outreach
- 📊 Liquidador (`liquidador-liftit`) — Sonnet — Diageo liquidation via Telegram
- 🔧 Diageo Liquidación (`diageo-liquidacion`) — no active session

### Diageo Service
- Earlier deployed `diageo-agent.service` via systemctl — running healthy
- Loads 230 routes, 922 deliveries, 306 unique services, cycles every 600s
