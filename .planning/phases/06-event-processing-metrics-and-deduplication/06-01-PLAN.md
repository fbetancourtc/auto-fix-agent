---
phase: 06-event-processing-metrics-and-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/lib/metrics.ts
  - api/lib/dedup.ts
  - api/lib/types.ts
  - package.json
  - vitest.config.ts
  - tests/metrics.test.ts
  - tests/dedup.test.ts
autonomous: true
requirements:
  - INFRA-03
  - OPS-01
  - OPS-02
  - OPS-04
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
user_setup:
  - service: upstash-redis
    why: "Deduplication store for GitHub webhook delivery IDs"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console -> Create Database (free tier) -> REST URL"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console -> Create Database (free tier) -> REST Token"
    dashboard_config:
      - task: "Create a free-tier Redis database"
        location: "https://console.upstash.com -> Create Database -> Region: us-east-1"

must_haves:
  truths:
    - "Metrics module exports typed functions for every metric category (ops health, value, safety)"
    - "Dedup module checks Redis for duplicate delivery IDs with fail-open semantics"
    - "All metric functions are tested against Sentry.metrics spy calls"
    - "Dedup is tested for new/duplicate/error scenarios"
  artifacts:
    - path: "api/lib/metrics.ts"
      provides: "Centralized metric emission functions"
      exports: ["emitTriggerCount", "emitOutcome", "emitRunDuration", "emitMTTR", "emitCostEstimate", "emitPrAccepted", "emitPrRejected", "emitCircuitBreakerTrip", "emitScopeViolation", "emitEscalation", "buildMetricTags", "FixOutcome", "MetricTags"]
    - path: "api/lib/dedup.ts"
      provides: "Redis-backed deduplication guard"
      exports: ["isDuplicate"]
    - path: "tests/metrics.test.ts"
      provides: "Unit tests for all metric emission functions"
    - path: "tests/dedup.test.ts"
      provides: "Unit tests for dedup guard"
  key_links:
    - from: "api/lib/metrics.ts"
      to: "@sentry/node"
      via: "Sentry.metrics.increment/distribution/gauge"
      pattern: "Sentry\\.metrics\\.(increment|distribution|gauge)"
    - from: "api/lib/dedup.ts"
      to: "@upstash/redis"
      via: "Redis SET NX with EX"
      pattern: "client\\.set.*nx.*ex"
    - from: "api/lib/metrics.ts"
      to: "config/repo-stack-map.json"
      via: "buildMetricTags reads repo config"
      pattern: "repoStackMap"
---

<objective>
Create the metrics module, dedup module, and test infrastructure for Phase 6.

Purpose: Establish the typed metric emission functions and dedup guard that handlers will call. Build with tests first so Plan 02 can wire handlers with confidence.
Output: `api/lib/metrics.ts`, `api/lib/dedup.ts`, test files, vitest config, `@upstash/redis` installed.
</objective>

<execution_context>
@/Users/fbetncourtc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fbetncourtc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-event-processing-metrics-and-deduplication/06-CONTEXT.md
@.planning/phases/06-event-processing-metrics-and-deduplication/06-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From api/lib/types.ts:
```typescript
export type WebhookEventType = 'workflow_run' | 'pull_request' | 'pull_request_review';
export interface ProcessEventResult { eventType: string; action: string; processed: boolean; reason?: string; }
export interface WebhookHeaders { eventType: string; deliveryId: string; signature: string; }
export function extractHeaders(headers: Headers): WebhookHeaders;
```

From api/lib/sentry.ts:
```typescript
import * as Sentry from '@sentry/node';
// Sentry.init() at module scope
export async function flushSentry(): Promise<void>;
```

From config/repo-stack-map.json (structure):
```json
{ "repos": { "fbetancourtc/repo-name": { "stack": "ts", "allowed_dirs": [...] }, ... } }
```

From config/pricing.json (structure):
```json
{ "monthly_budget_usd": 200, "alert_thresholds": [0.5, 0.8, 1.0] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, create vitest config, and build metrics module with tests</name>
  <files>package.json, vitest.config.ts, api/lib/metrics.ts, api/lib/types.ts, tests/metrics.test.ts</files>
  <behavior>
    - emitTriggerCount calls Sentry.metrics.increment('auto_fix.trigger_count', 1, { tags: {repo, org, stack} })
    - emitOutcome calls increment('auto_fix.outcome', 1, { tags: {repo, org, stack, outcome} }) for each of 5 FixOutcome values
    - emitRunDuration calls distribution('auto_fix.run_duration_ms', N, { tags, unit: 'millisecond' })
    - emitMTTR calls distribution('auto_fix.mttr_ms', N, { tags, unit: 'millisecond' })
    - emitCostEstimate calls distribution('auto_fix.cost_per_fix_usd', N, { tags }) AND gauge('auto_fix.monthly_spend_usd', N, { tags })
    - emitPrAccepted/emitPrRejected call increment with correct metric names
    - emitCircuitBreakerTrip/emitScopeViolation/emitEscalation call increment('auto_fix.safety.*', ...)
    - buildMetricTags('fbetancourtc/some-repo') returns { repo, org: 'fbetancourtc', stack } from repo-stack-map.json; unknown repo returns stack: 'unknown'
    - computeMttrMs returns positive ms for valid inputs, null for negative or >24h
    - estimateCostUsd returns BASE_COST + RATE_PER_MIN * durationMin
  </behavior>
  <action>
    1. Run `npm install --save @upstash/redis` and `npm install --save-dev vitest`
    2. Create `vitest.config.ts`: minimal ESM config with `test: { include: ['tests/**/*.test.ts'] }`
    3. Add `"test": "vitest run --reporter=verbose"` to package.json scripts
    4. Add to `api/lib/types.ts`: export `FixOutcome` type (union of 5 strings) and `MetricTags` interface ({ repo: string; org: string; stack: string })
    5. Create `api/lib/metrics.ts` following the RESEARCH.md Pattern 1 and Pattern 3 exactly:
       - Import `* as Sentry from '@sentry/node'`
       - Import repo-stack-map.json using `fs.readFileSync` + JSON.parse at module scope (avoid JSON import assertion issues per RESEARCH.md Pitfall 4)
       - Export `buildMetricTags(repoFullName: string): MetricTags` -- splits org from full_name, looks up stack in repo-stack-map
       - Export `computeMttrMs(prCreatedAt: string, workflowRunStartedAt: string): number | null` -- per RESEARCH.md code example
       - Export `estimateCostUsd(runDurationMs: number): number` -- per RESEARCH.md code example (BASE=0.50, RATE=0.10/min)
       - Export all emit* functions per RESEARCH.md Pattern 1: emitTriggerCount, emitOutcome, emitRunDuration, emitMTTR, emitCostEstimate, emitPrAccepted, emitPrRejected, emitCircuitBreakerTrip, emitScopeViolation, emitEscalation
       - Each emit* function takes MetricTags (and relevant value params) and calls the appropriate Sentry.metrics.* method
    6. Create `tests/metrics.test.ts`:
       - Mock `@sentry/node` with vi.mock -- spy on Sentry.metrics.increment, .distribution, .gauge
       - Mock `fs.readFileSync` to return a test repo-stack-map fixture
       - Test each emit* function verifies correct Sentry.metrics call with exact metric name, value, tags
       - Test buildMetricTags with known and unknown repos
       - Test computeMttrMs with valid, negative, and >24h cases
       - Test estimateCostUsd with sample durations
    7. Run `npx vitest run tests/metrics.test.ts` -- RED then GREEN cycle
  </action>
  <verify>
    <automated>npx vitest run tests/metrics.test.ts --reporter=verbose</automated>
  </verify>
  <done>metrics.ts exports all typed emit functions + helpers, all tests pass, vitest config works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build dedup module with tests</name>
  <files>api/lib/dedup.ts, tests/dedup.test.ts</files>
  <behavior>
    - isDuplicate returns false for new delivery IDs (Redis SET NX returns 'OK')
    - isDuplicate returns true for already-seen delivery IDs (Redis SET NX returns null)
    - isDuplicate returns false (fail-open) when Redis throws an error
    - isDuplicate returns false when Redis env vars are not configured (no client)
    - Redis key format is 'dedup:{deliveryId}' with EX 86400 (24h TTL)
  </behavior>
  <action>
    1. Create `api/lib/dedup.ts` following RESEARCH.md Pattern 2 exactly:
       - Import `{ Redis } from '@upstash/redis'`
       - Lazy singleton `getRedis()` reading UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN from process.env
       - Returns null if env vars missing
       - `isDuplicate(deliveryId: string): Promise<boolean>` -- SET NX with EX 86400
       - Returns true if key existed (duplicate), false if key was set (new)
       - try/catch returns false on any error (fail-open)
    2. Create `tests/dedup.test.ts`:
       - Mock `@upstash/redis` with vi.mock -- mock Redis constructor and .set() method
       - Test: new delivery ID -> set returns 'OK' -> isDuplicate returns false
       - Test: duplicate delivery ID -> set returns null -> isDuplicate returns true
       - Test: Redis error -> isDuplicate returns false (fail-open)
       - Test: no env vars -> isDuplicate returns false
       - Verify key format is `dedup:{id}` and options include `{ nx: true, ex: 86400 }`
    3. Run `npx vitest run tests/dedup.test.ts` -- RED then GREEN cycle
  </action>
  <verify>
    <automated>npx vitest run tests/dedup.test.ts --reporter=verbose</automated>
  </verify>
  <done>dedup.ts exports isDuplicate with fail-open semantics, all tests pass, Redis key uses 24h TTL</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
npx tsc --noEmit
</verification>

<success_criteria>
- vitest runs and all tests in tests/metrics.test.ts and tests/dedup.test.ts pass
- `npx tsc --noEmit` passes with no type errors
- api/lib/metrics.ts exports all 10 emit* functions + buildMetricTags + computeMttrMs + estimateCostUsd
- api/lib/dedup.ts exports isDuplicate with fail-open pattern
- @upstash/redis is in package.json dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/06-event-processing-metrics-and-deduplication/06-01-SUMMARY.md`
</output>
