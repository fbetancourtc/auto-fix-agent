---
phase: 06-event-processing-metrics-and-deduplication
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - api/webhook.ts
autonomous: false
requirements:
  - HOOK-04
  - OPS-03

must_haves:
  truths:
    - "Redelivering the same GitHub webhook event does not trigger handler processing a second time"
    - "Dedup check runs inside processEvent before routeEvent is called"
    - "When Redis is unavailable, events are still processed (fail-open)"
    - "Duplicate deliveries produce a Sentry breadcrumb for observability"
  artifacts:
    - path: "api/webhook.ts"
      provides: "Dedup integration point"
      contains: "isDuplicate"
  key_links:
    - from: "api/webhook.ts"
      to: "api/lib/dedup.ts"
      via: "import { isDuplicate } from './lib/dedup.js'"
      pattern: "isDuplicate.*deliveryId"
---

<objective>
Integrate the dedup guard into processEvent() and verify the full pipeline works end-to-end with a deployment checkpoint.

Purpose: Wire the dedup module into the main webhook entry point so duplicate GitHub deliveries are caught before any handler runs. Verify deployment readiness.
Output: Updated api/webhook.ts with dedup integration, human-verified deployment.
</objective>

<execution_context>
@/Users/fbetncourtc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fbetncourtc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-event-processing-metrics-and-deduplication/06-CONTEXT.md
@.planning/phases/06-event-processing-metrics-and-deduplication/06-RESEARCH.md
@.planning/phases/06-event-processing-metrics-and-deduplication/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 dedup module -->
From api/lib/dedup.ts:
```typescript
export async function isDuplicate(deliveryId: string): Promise<boolean>;
```

From api/webhook.ts (current processEvent flow):
```typescript
// verify signature -> respond 200 -> waitUntil(processEvent(headers, payload))
// processEvent: extractHeaders -> routeEvent(eventType, payload)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate dedup check into processEvent</name>
  <files>api/webhook.ts</files>
  <action>
    1. Add import: `import { isDuplicate } from './lib/dedup.js'`
    2. In `processEvent()`, BEFORE the `routeEvent()` call, add the dedup check per RESEARCH.md Pattern:
       ```
       if (await isDuplicate(headers.deliveryId)) {
         Sentry.addBreadcrumb({
           category: 'webhook.dedup',
           message: `Duplicate delivery skipped: ${headers.deliveryId}`,
         });
         return; // Skip all processing for duplicates
       }
       ```
    3. The dedup check happens inside `waitUntil()` (already post-response), so it does NOT block the HTTP response (per RESEARCH.md anti-patterns section)
    4. Keep all existing code intact -- this is a single insertion point
    5. Ensure the return type of processEvent still satisfies its callers (void or ProcessEventResult -- if it returns ProcessEventResult, return a result with processed: false, reason: 'duplicate delivery')
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>processEvent checks isDuplicate before routeEvent. Duplicates are skipped with a breadcrumb. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 6 implementation: metrics module, dedup guard, strong-typed handlers, and dedup integration in processEvent. The webhook receiver now emits structured Sentry metrics for every auto-fix event and deduplicates redelivered webhooks via Upstash Redis.</what-built>
  <how-to-verify>
    1. Run `npx tsc --noEmit` -- should pass with zero errors
    2. Run `npx vitest run --reporter=verbose` -- all tests should pass
    3. Review api/lib/metrics.ts -- confirm all emit* functions exist
    4. Review api/lib/dedup.ts -- confirm fail-open pattern
    5. Review api/webhook.ts -- confirm isDuplicate check before routeEvent
    6. Review handlers -- confirm strong types (WorkflowRunEvent etc.) and metric calls
    7. If Upstash Redis credentials are configured: run `vercel dev` and send a test webhook to verify dedup works locally
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npx tsc --noEmit
npx vitest run --reporter=verbose
</verification>

<success_criteria>
- processEvent() calls isDuplicate(headers.deliveryId) before routeEvent()
- Duplicate deliveries are short-circuited with a Sentry breadcrumb
- All TypeScript compiles clean
- All vitest tests pass
- Human approves the complete Phase 6 implementation
</success_criteria>

<output>
After completion, create `.planning/phases/06-event-processing-metrics-and-deduplication/06-03-SUMMARY.md`
</output>
