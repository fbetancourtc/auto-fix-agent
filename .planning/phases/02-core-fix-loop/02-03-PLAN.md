---
phase: 02-core-fix-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - prompts/typescript.md
  - prompts/python.md
  - prompts/kotlin.md
  - .github/workflows/auto-fix.yml
autonomous: true
requirements:
  - PRMG-01
  - PRMG-02
  - PRMG-03
  - PRMG-04
  - PRMG-05

must_haves:
  truths:
    - "All three stack prompts instruct the agent to create an auto-fix/{run-id} branch, commit with conventional commits, and NOT push or create the PR"
    - "All three stack prompts instruct the agent to check retry count via gh pr list before attempting a fix"
    - "All three stack prompts instruct the agent to create a needs-human escalation issue when retry count >= 2, with links to prior PRs and full sanitized CI logs"
    - "The PR description includes root cause analysis, changes made, and verification results (structured sections)"
    - "No gh pr merge command exists anywhere in allowedTools or prompts -- human review gate enforced by omission"
    - "The auto-fix.yml workflow passes RUN_ID and HEAD_BRANCH as environment variables to the agent step for use in branch naming and retry correlation"
  artifacts:
    - path: "prompts/typescript.md"
      provides: "Complete TypeScript fix prompt with git workflow, retry guard, PR description template, and escalation instructions"
      min_lines: 80
    - path: "prompts/python.md"
      provides: "Complete Python fix prompt with git workflow, retry guard, PR description template, and escalation instructions"
      min_lines: 50
    - path: "prompts/kotlin.md"
      provides: "Complete Kotlin fix prompt with git workflow, retry guard, PR description template, and escalation instructions"
      min_lines: 50
    - path: ".github/workflows/auto-fix.yml"
      provides: "Workflow passes RUN_ID and HEAD_BRANCH env vars to agent step"
      contains: "RUN_ID"
  key_links:
    - from: "prompts/typescript.md"
      to: "gh pr list"
      via: "Retry guard instructions tell agent to count auto-fix PRs before fixing"
      pattern: "gh pr list"
    - from: "prompts/typescript.md"
      to: "gh issue create"
      via: "Escalation instructions tell agent to create needs-human issue after 2 failures"
      pattern: "gh issue create"
    - from: "prompts/typescript.md"
      to: "git checkout -b"
      via: "Branch creation instructions for auto-fix/{run-id} naming"
      pattern: "auto-fix/"
    - from: ".github/workflows/auto-fix.yml"
      to: "prompts/*.md"
      via: "Workflow env vars (RUN_ID, HEAD_BRANCH, TARGET_REPO) available to agent for branch naming and retry correlation"
      pattern: "RUN_ID"
---

<objective>
Update all three stack prompts with git workflow instructions, retry guard logic, human escalation procedures, and structured PR description requirements. Wire the workflow to pass run context to the agent.

Purpose: The agent needs to know how to create fix branches, check retry counts, escalate to humans, and structure its commit messages with root cause analysis. These instructions complete the fix loop from "agent diagnoses failure" to "PR created or human escalated."
Output: Updated prompts for all three stacks with complete fix-loop instructions, and workflow env vars for agent context.
</objective>

<execution_context>
@/Users/fbetncourtc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fbetncourtc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-fix-loop/02-RESEARCH.md
@.planning/phases/02-core-fix-loop/02-02-SUMMARY.md

Key architecture after Plans 02-01 and 02-02:
- auto-fix.yml has flakiness filter, head_branch input, diff validation step, and workflow-controlled push/PR creation
- Agent has allowedTools: Edit, Read, Write, test/lint commands, git (checkout -b, add, status, diff, commit), gh (pr list, issue create)
- Agent does NOT have git push or gh pr create -- workflow handles these after diff validation
- Agent has GH_TOKEN env var (repo-scoped app token) for gh CLI operations
- Diff validation runs after agent, reverts forbidden files, blocks push if no valid changes
- PR creation step pushes auto-fix/{run-id} branch and creates PR targeting develop

Key constraints from CONTEXT.md:
- Branch naming: auto-fix/{run-id}
- Commit messages: conventional commits (fix(scope): description)
- PR targets develop branch with auto-fix label
- PR description: Root Cause Analysis, Changes Made, Verification Results sections
- Agent checks retry count via gh pr list -- count auto-fix PRs targeting develop
- Max 2 fix attempts per failure
- After 2 failed attempts: create GitHub Issue on failing repo with needs-human label
- Issue includes: failure summary, links to both attempt PRs, link to original CI run, full sanitized CI logs
- PRMG-05: No auto-merge -- enforced by NOT including gh pr merge in allowedTools (already satisfied)

Agent environment variables available (from workflow):
- GH_TOKEN: repo-scoped app token for gh CLI
- RUN_ID: failed workflow run ID (to be added this plan)
- HEAD_BRANCH: branch where CI failed (to be added this plan)
- TARGET_REPO: owner/repo format (to be added this plan)
</context>

<interfaces>
<!-- Current prompt structure (all three stacks follow same pattern): -->
<!-- # Auto-Fix Agent: {Stack} Stack -->
<!-- ## Context -->
<!-- ## Instructions (numbered) -->
<!-- ## Constraints (bullet list with allowed/forbidden dirs) -->
<!-- ## Common Failure Patterns (stack-specific) -->
<!-- ## Output -->

<!-- New sections to add (after Constraints, before Common Failure Patterns): -->
<!-- ## Git Workflow -->
<!-- ## Retry Guard -->
<!-- ## Escalation -->
<!-- ## PR Description -->

<!-- Environment variables the agent can access: -->
<!-- $RUN_ID - failed workflow run ID (for branch naming) -->
<!-- $TARGET_REPO - owner/repo (for gh CLI --repo flag) -->
<!-- $HEAD_BRANCH - branch where CI failed (for context) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add RUN_ID, HEAD_BRANCH, TARGET_REPO env vars to agent step in auto-fix.yml</name>
  <files>.github/workflows/auto-fix.yml</files>
  <action>
Update the Claude Code Action step's `env:` block in `.github/workflows/auto-fix.yml` to pass run context to the agent.

The step (after Plan 02-02) already has:
```yaml
env:
  GH_TOKEN: ${{ steps.app-token.outputs.token }}
```

Extend it to:
```yaml
env:
  GH_TOKEN: ${{ steps.app-token.outputs.token }}
  RUN_ID: ${{ inputs.failed_run_id }}
  HEAD_BRANCH: ${{ inputs.head_branch }}
  TARGET_REPO: ${{ inputs.repository }}
```

These env vars are used by the agent (via prompt instructions) for:
- `RUN_ID`: Branch naming (`auto-fix/{RUN_ID}`) and linking to the original CI run
- `HEAD_BRANCH`: Context about which branch failed
- `TARGET_REPO`: The `--repo` flag for gh CLI commands (`gh pr list --repo "$TARGET_REPO"`)

This is a small, targeted change to the existing env block. Do NOT modify any other part of the step.
  </action>
  <verify>
    <automated>grep -A5 "GH_TOKEN" .github/workflows/auto-fix.yml | grep -q "RUN_ID" && grep -A6 "GH_TOKEN" .github/workflows/auto-fix.yml | grep -q "HEAD_BRANCH" && grep -A7 "GH_TOKEN" .github/workflows/auto-fix.yml | grep -q "TARGET_REPO" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Claude Code Action step env block has RUN_ID, HEAD_BRANCH, and TARGET_REPO in addition to GH_TOKEN
    - All values use inputs. references (not inline expressions with untrusted content)
    - No other changes to the step or workflow
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all stack prompts with git workflow, retry guard, and escalation instructions</name>
  <files>prompts/typescript.md, prompts/python.md, prompts/kotlin.md</files>
  <action>
Update all three stack prompt files to add four new sections: Git Workflow, Retry Guard, Escalation, and PR Description. Insert these sections AFTER the existing "Constraints" section and BEFORE the existing "Common Failure Patterns" section (for typescript.md) or "Common Patterns" section (for kotlin.md and python.md).

The new sections are IDENTICAL across all three stacks (they are stack-agnostic). Add the following content to each prompt file:

```markdown
## Before You Start

Before implementing any fix, you MUST complete these checks in order:

### 1. Check Retry Count

Run this command to count previous auto-fix attempts for this failure:

```bash
ATTEMPT_COUNT=$(gh pr list --repo "$TARGET_REPO" --label auto-fix --state all \
  --json headRefName \
  --jq '[.[] | select(.headRefName | startswith("auto-fix/"))] | length')
echo "Previous auto-fix attempts: $ATTEMPT_COUNT"
```

- If `ATTEMPT_COUNT >= 2`: Do NOT attempt a fix. Skip to the **Escalation** section below.
- If `ATTEMPT_COUNT < 2`: Proceed with the fix.

### 2. Create Fix Branch

```bash
git checkout -b "auto-fix/${RUN_ID}"
```

This creates the fix branch from the current HEAD (which is the failing branch, checked out by the workflow).

## Fix Workflow

After diagnosing the failure and implementing your fix:

1. Run the appropriate test/lint commands to verify your fix works
2. If tests still fail after your fix, **stop** -- do not commit. The workflow will count this as a failed attempt.
3. If tests pass, stage and commit your changes:

```bash
git add -A
git commit -m "fix(<scope>): <brief description of the fix>

Root Cause: <one-line explanation of why CI failed>
Changes: <one-line summary of what was changed>
Verified: <which test/lint command confirmed the fix>"
```

Use conventional commits format. The `<scope>` should be the module or feature area (e.g., `auth`, `api`, `ui`, `lint`).

**IMPORTANT:** Do NOT run `git push` or `gh pr create`. The workflow handles pushing and PR creation after validating your changes.

## Escalation

If the retry count check showed `ATTEMPT_COUNT >= 2`, create an escalation issue instead of attempting a fix:

```bash
# Get links to previous auto-fix PRs
PREV_PRS=$(gh pr list --repo "$TARGET_REPO" --label auto-fix --state all \
  --json number,url,headRefName \
  --jq '[.[] | select(.headRefName | startswith("auto-fix/")) | "- PR #\(.number): \(.url)"] | join("\n")')

# Read sanitized CI logs
CI_LOGS=$(cat /tmp/ci-logs.txt 2>/dev/null || echo "CI logs not available")

gh issue create --repo "$TARGET_REPO" \
  --label "needs-human" \
  --title "CI fix failed after 2 attempts: ${HEAD_BRANCH}" \
  --body "## Auto-Fix Escalation

**Status:** 2 fix attempts failed -- needs human review

### Failure Context
- **Branch:** \`${HEAD_BRANCH}\`
- **CI Run:** [#${RUN_ID}](https://github.com/${TARGET_REPO}/actions/runs/${RUN_ID})

### Previous Fix Attempts
${PREV_PRS}

### CI Failure Logs
<details>
<summary>Full sanitized CI logs (click to expand)</summary>

\`\`\`
${CI_LOGS}
\`\`\`

</details>

---
*Escalated by [auto-fix-agent](https://github.com/fbetancourtc/auto-fix-agent)*"
```

After creating the escalation issue, **stop**. Do not attempt any fix.
```

Additionally, update the existing **Output** section at the end of each prompt to reference the commit message format:

Replace the current Output section content with:

```markdown
## Output

After applying your fix:
1. Run the appropriate verification command to confirm the fix works
2. If verification passes, commit using the format specified in "Fix Workflow" above
3. If verification fails, stop without committing -- do not push broken code
4. Report what files you changed and why (this information is captured in your commit message)
```

**Stack-specific notes:**

For `prompts/typescript.md`:
- The new sections go between "## Constraints" and "## Common Failure Patterns"
- Keep all existing content in Context, Instructions, Constraints, Common Failure Patterns intact
- Only add the new sections and update the Output section

For `prompts/python.md`:
- The new sections go between "## Constraints" and "## Common Patterns"
- Keep all existing content intact
- The `<!-- TODO -->` comment should remain

For `prompts/kotlin.md`:
- The new sections go between "## Constraints" and "## Common Patterns"
- Keep all existing content intact
- The `<!-- TODO -->` comment should remain

CRITICAL: The retry guard, escalation, and git workflow sections use environment variables ($RUN_ID, $TARGET_REPO, $HEAD_BRANCH) that are available because the workflow sets them in the agent step's env block (Task 1 of this plan).
  </action>
  <verify>
    <automated>for f in prompts/typescript.md prompts/python.md prompts/kotlin.md; do echo "--- $f ---"; grep -q "Retry Count" "$f" && echo "Retry: PASS" || echo "Retry: FAIL"; grep -q "auto-fix/" "$f" && echo "Branch: PASS" || echo "Branch: FAIL"; grep -q "gh issue create" "$f" && echo "Escalation: PASS" || echo "Escalation: FAIL"; grep -q "needs-human" "$f" && echo "Label: PASS" || echo "Label: FAIL"; grep -q "git commit" "$f" && echo "Commit: PASS" || echo "Commit: FAIL"; grep -q "Do NOT run.*git push" "$f" && echo "No-push: PASS" || echo "No-push: FAIL"; done</automated>
  </verify>
  <done>
    - All three prompts have "Before You Start" section with retry count check
    - All three prompts instruct agent to create auto-fix/{RUN_ID} branch
    - All three prompts have "Fix Workflow" section with conventional commit format and explicit "do NOT push" instruction
    - All three prompts have "Escalation" section that creates needs-human issue with prior PR links, CI logs, and run link
    - All three prompts have updated Output section referencing commit format
    - Existing stack-specific content (Context, Instructions, Constraints, Common Patterns) is preserved
    - No mention of gh pr merge anywhere in any prompt (PRMG-05 enforced by omission)
    - Environment variables ($RUN_ID, $TARGET_REPO, $HEAD_BRANCH) referenced correctly
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify env vars in workflow: `grep -A8 "env:" .github/workflows/auto-fix.yml | grep -c "RUN_ID\|HEAD_BRANCH\|TARGET_REPO"` returns 3
2. Verify retry guard in prompts: `grep -l "ATTEMPT_COUNT" prompts/*.md | wc -l` returns 3
3. Verify escalation in prompts: `grep -l "needs-human" prompts/*.md | wc -l` returns 3
4. Verify branch naming in prompts: `grep -l 'auto-fix/\${RUN_ID}' prompts/*.md | wc -l` returns 3
5. Verify no push instruction: `grep -l "Do NOT run.*git push" prompts/*.md | wc -l` returns 3
6. Verify no merge anywhere: `grep -rl "gh pr merge" prompts/ .github/workflows/auto-fix.yml; echo "PASS if empty above"` returns empty
7. Verify prompt line counts: `wc -l prompts/*.md` -- typescript >= 80, python >= 50, kotlin >= 50
8. Validate YAML: `python3 -c "import sys,yaml; yaml.safe_load(open('.github/workflows/auto-fix.yml').read()); print('Valid YAML')"`
</verification>

<success_criteria>
- Auto-created fix PR has auto-fix label and targets develop (PRMG-01 -- workflow creates PR after agent commits)
- PR description includes root cause analysis, changes, and verification (PRMG-02 -- via agent commit message and workflow PR template)
- Retry guard limits to 2 attempts per failure via gh pr list check in agent prompt (PRMG-03)
- After 2 failures, agent creates needs-human issue with failure context, PR links, and CI logs (PRMG-04)
- No gh pr merge exists anywhere -- human review gate enforced architecturally (PRMG-05)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-fix-loop/02-03-SUMMARY.md`
</output>
