---
phase: 07-dashboard-cron-monitors-and-alert-rules
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/setup-dashboard.sh
  - scripts/setup-alerts.sh
  - docs/sentry-setup.md
autonomous: false
requirements:
  - SENT-02
  - SENT-04
user_setup:
  - service: sentry
    why: "Dashboard creation, alert rule configuration, and API token for setup scripts"
    env_vars:
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry -> Settings -> Developer Settings -> Internal Integrations -> Create token with org:write, alerts:write, project:write scopes"
      - name: SENTRY_ORG_SLUG
        source: "Sentry -> Settings -> General -> Organization Slug"
      - name: SENTRY_PROJECT_SLUG
        source: "Sentry -> Settings -> Projects -> auto-fix-monitor (or project name)"
    dashboard_config:
      - task: "Verify Sentry plan supports custom metrics in dashboards and metric alert rules"
        location: "Sentry -> Settings -> Subscription"
must_haves:
  truths:
    - "A Sentry Custom Dashboard exists with Operations Health, Value Metrics, and Safety Signal panels"
    - "Sentry alert rules fire when success rate drops, cost spikes, or a repo goes silent"
    - "Setup scripts document the exact Sentry configuration for reproducibility"
  artifacts:
    - path: "scripts/setup-dashboard.sh"
      provides: "Sentry Dashboard API creation script with all 12 widget definitions"
      min_lines: 50
    - path: "scripts/setup-alerts.sh"
      provides: "Sentry Alert Rule API creation script for 4 alert rules"
      min_lines: 40
    - path: "docs/sentry-setup.md"
      provides: "Manual setup guide as fallback for API scripts"
      min_lines: 30
  key_links:
    - from: "scripts/setup-dashboard.sh"
      to: "Sentry Dashboard API"
      via: "curl POST /api/0/organizations/{org}/dashboards/"
      pattern: "sentry.io/api"
    - from: "scripts/setup-alerts.sh"
      to: "Sentry Alert Rules API"
      via: "curl POST /api/0/organizations/{org}/alert-rules/"
      pattern: "alert-rules"
---

<objective>
Create Sentry dashboard with operational panels and alert rules for threshold-based notifications, with reproducible setup scripts.

Purpose: SENT-02 requires a dashboard with 3 panel groups (Ops Health, Value Metrics, Safety Signals). SENT-04 requires alert rules for success rate drops, cost spikes, and repo silence. These are Sentry platform configurations, not application code.
Output: Setup scripts for dashboard and alert rules, manual setup documentation, and verified Sentry configuration.
</objective>

<execution_context>
@/home/felipe/.claude/get-shit-done/workflows/execute-plan.md
@/home/felipe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard-cron-monitors-and-alert-rules/07-RESEARCH.md
@.planning/phases/07-dashboard-cron-monitors-and-alert-rules/07-01-SUMMARY.md

<interfaces>
<!-- Metric inventory from api/lib/metrics.ts - these are the data sources for dashboard widgets -->

Counters (MRI: c:custom/...@none):
- auto_fix.trigger_count (tags: repo, org, stack)
- auto_fix.outcome (tags: repo, org, stack, outcome)
- auto_fix.pr_accepted (tags: repo, org, stack)
- auto_fix.pr_rejected (tags: repo, org, stack)
- auto_fix.safety.circuit_breaker_trip (tags: repo, org, stack)
- auto_fix.safety.scope_violation (tags: repo, org, stack)
- auto_fix.safety.escalation (tags: repo, org, stack)

Distributions (MRI: d:custom/...):
- auto_fix.run_duration_ms (unit: millisecond, tags: repo, org, stack)
- auto_fix.mttr_ms (unit: millisecond, tags: repo, org, stack)
- auto_fix.cost_per_fix_usd (tags: repo, org, stack)

Gauges (MRI: g:custom/...@none):
- auto_fix.monthly_spend_usd (tags: repo, org, stack)

Dashboard Panel Design (from RESEARCH.md):
- Operations Health: trigger frequency (bar), outcome breakdown (stacked bar), run duration p50/p95 (line), per-repo health (table)
- Value Metrics: MTTR trend (line), cost per fix (line), monthly spend (big number), PR acceptance rate (big number)
- Safety Signals: budget burn rate (line), circuit breaker trips (big number), scope violations (big number), escalations (big number)

Alert Rules Design (from RESEARCH.md):
- Low Success Rate: outcome metric, below threshold, 24h window, warning
- Cost Spike: cost_per_fix_usd, above $5.00, 1h window, warning
- Budget Warning (80%): monthly_spend_usd, above $160, 24h window, warning
- Budget Critical (100%): monthly_spend_usd, above $200, 24h window, critical
- Repo Silence: handled by cron monitors (Plan 01), not alert rules
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sentry setup scripts and documentation</name>
  <files>scripts/setup-dashboard.sh, scripts/setup-alerts.sh, docs/sentry-setup.md</files>
  <action>
    Create three files that serve as Infrastructure-as-Code artifacts for the Sentry configuration.

    **scripts/setup-dashboard.sh:**
    - Bash script using curl to create the Sentry Custom Dashboard via API
    - Requires env vars: SENTRY_AUTH_TOKEN, SENTRY_ORG_SLUG
    - Creates dashboard titled "Auto-Fix Operations" with 3 sections:
      - Operations Health: 4 widgets (trigger count bar by repo, outcome stacked bar, run duration line p50/p95, per-repo health table)
      - Value Metrics: 4 widgets (MTTR trend line, cost per fix line, monthly spend big number, PR acceptance rate big number)
      - Safety Signals: 4 widgets (budget burn rate line, circuit breaker trips big number, scope violations big number, escalations big number)
    - Use the MRI format from RESEARCH.md metric inventory (e.g., `c:custom/auto_fix.trigger_count@none`)
    - Include a comment noting LOW confidence on exact MRI format -- user should verify first widget works, then adjust remaining if needed
    - Include a fallback comment: "If API creation fails, see docs/sentry-setup.md for manual UI setup"
    - Make executable (chmod +x in script header comment)

    **scripts/setup-alerts.sh:**
    - Bash script using curl to create 4 metric alert rules via Sentry Alert Rules API
    - Requires env vars: SENTRY_AUTH_TOKEN, SENTRY_ORG_SLUG, SENTRY_PROJECT_SLUG
    - Alert 1: "Auto-Fix: Low Success Rate" -- count of outcome=fix_pr_created below threshold, 24h window, warning
    - Alert 2: "Auto-Fix: Cost Spike" -- avg cost_per_fix_usd above $5.00, 1h window, warning
    - Alert 3: "Auto-Fix: Budget Warning (80%)" -- sum monthly_spend_usd above $160, 24h window, warning
    - Alert 4: "Auto-Fix: Budget Critical (100%)" -- sum monthly_spend_usd above $200, 24h window, critical
    - Use dataset "generic_metrics" for all rules (RESEARCH.md Pitfall 4)
    - Email notification action targeting the authenticated user
    - Make executable

    **docs/sentry-setup.md:**
    - Manual setup guide as fallback when API scripts fail (RESEARCH.md notes LOW confidence on MRI format)
    - Section 1: Prerequisites (Sentry plan requirements, auth token creation with required scopes)
    - Section 2: Dashboard Setup (step-by-step UI instructions for creating each panel group with metric picker)
    - Section 3: Alert Rules Setup (step-by-step UI instructions for each alert rule)
    - Section 4: Cron Monitor Verification (how to verify monitors appear after first webhook events)
    - Section 5: Troubleshooting (empty widgets, monitors not appearing, MRI format issues)
    - Reference the metric inventory table from RESEARCH.md
    - Note: Repo silence alerting is automatic via cron monitors (Plan 01) -- no separate alert rule needed

    All scripts should have proper error handling (set -euo pipefail), usage instructions, and clear comments.
  </action>
  <verify>
    <automated>bash -n scripts/setup-dashboard.sh && bash -n scripts/setup-alerts.sh && test -f docs/sentry-setup.md && echo "All files valid"</automated>
  </verify>
  <done>Three files created: setup-dashboard.sh and setup-alerts.sh are valid bash scripts, sentry-setup.md provides comprehensive manual fallback instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy and verify Sentry configuration</name>
  <files>N/A</files>
  <action>
    Deploy updated webhook function and verify all three Sentry configurations are operational.

    The cron monitors auto-create in Sentry on first webhook event per repo (via captureCheckIn with upsert behavior).

    Human verification steps:
    1. Deploy the updated webhook function: `npx vercel --prod`
    2. Trigger a test webhook from any enrolled repo (or wait for next CI event)
    3. Check Sentry Cron Monitors: Settings -> Cron Monitors -- verify a monitor appeared for the repo
    4. Run setup scripts (optional, may need MRI adjustments):
       - `SENTRY_AUTH_TOKEN=xxx SENTRY_ORG_SLUG=xxx ./scripts/setup-dashboard.sh`
       - `SENTRY_AUTH_TOKEN=xxx SENTRY_ORG_SLUG=xxx SENTRY_PROJECT_SLUG=xxx ./scripts/setup-alerts.sh`
    5. If scripts fail on MRI format, use manual setup from `docs/sentry-setup.md` via Sentry UI
    6. Verify dashboard shows widgets (may show "No data" until events flow)
    7. Verify alert rules exist in Sentry -> Alerts -> Alert Rules
  </action>
  <verify>User confirms Sentry dashboard, cron monitors, and alert rules are configured</verify>
  <done>Sentry Custom Dashboard has 3 panel groups with live widgets. Cron monitors appear for repos that have processed events. Alert rules exist for success rate, cost, and budget thresholds.</done>
</task>

</tasks>

<verification>
- `bash -n scripts/setup-dashboard.sh` -- valid bash syntax
- `bash -n scripts/setup-alerts.sh` -- valid bash syntax
- `docs/sentry-setup.md` exists with manual setup instructions
- Sentry Cron Monitor appears after first webhook event (manual check)
- Sentry Dashboard created with 3 panel groups (manual check)
- Sentry Alert Rules created for 4 threshold conditions (manual check)
</verification>

<success_criteria>
- Setup scripts exist and have valid bash syntax
- Manual setup documentation covers all three Sentry configuration areas
- User confirms Sentry dashboard, cron monitors, and alert rules are operational
- v1.2 milestone is complete: full pipeline from webhook receiver to actionable visibility
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-cron-monitors-and-alert-rules/07-02-SUMMARY.md`
</output>
