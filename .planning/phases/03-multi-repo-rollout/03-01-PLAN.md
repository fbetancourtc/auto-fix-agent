---
phase: 03-multi-repo-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prompts/python.md
  - ONBOARDING.md
autonomous: true
requirements:
  - PYTHON-PROMPT
  - KOTLIN-PROMPT
  - ONBOARDING

must_haves:
  truths:
    - "Python prompt contains 6 comprehensive failure pattern categories matching TypeScript prompt depth"
    - "Kotlin prompt stub is unchanged (user decision: expand when real failures surface)"
    - "ONBOARDING.md provides complete setup guide for enrolling a new repo"
    - "Onboarding docs cover secrets config, caller deployment, workflow name customization, and smoke test procedure"
  artifacts:
    - path: "prompts/python.md"
      provides: "Expanded Python fix prompt with 6 failure pattern categories"
      contains: "Import Errors|Missing Dependencies|Fixture Issues|Async/Await|Pydantic v2|Ruff Lint"
    - path: "ONBOARDING.md"
      provides: "Complete onboarding guide for new repo enrollment"
      contains: "Prerequisites|Configure Secrets|Deploy Caller|Verify Setup|Troubleshooting"
    - path: "prompts/kotlin.md"
      provides: "Unchanged Kotlin stub prompt"
      contains: "TODO: Expand"
  key_links:
    - from: "ONBOARDING.md"
      to: "auto-fix-caller.example.yml"
      via: "References the caller template for deployment"
      pattern: "auto-fix-caller\\.example\\.yml"
    - from: "ONBOARDING.md"
      to: "config/repo-stack-map.json"
      via: "Explains allowed_dirs customization"
      pattern: "repo-stack-map"
---

<objective>
Expand the Python fix prompt with a comprehensive failure pattern library and create onboarding documentation for new repo enrollment.

Purpose: The Python prompt is currently a stub with 4 bullet points. The TypeScript prompt has 16 detailed patterns across 4 categories. The Python prompt needs equivalent depth (6 categories, ~24 patterns) to effectively guide the AI agent on Python CI failures. The onboarding doc enables self-service enrollment for any repo. The Kotlin prompt is intentionally left as-is per user decision.

Output: Expanded `prompts/python.md`, new `ONBOARDING.md` at repo root, unchanged `prompts/kotlin.md` (verified).
</objective>

<execution_context>
@/Users/fbetncourtc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fbetncourtc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-repo-rollout/03-CONTEXT.md
@.planning/phases/03-multi-repo-rollout/03-RESEARCH.md

@prompts/python.md
@prompts/typescript.md
@prompts/kotlin.md
@.github/workflows/auto-fix-caller.example.yml
@config/repo-stack-map.json

<interfaces>
<!-- The executor needs to understand the existing prompt structure to expand consistently -->

From prompts/typescript.md (reference for pattern depth):
- 4 categories: Vitest Test Failures, ESLint Errors, TypeScript Errors, Build Errors
- Each category has 4 sub-patterns with bold keyword + colon + detailed guidance
- Total: ~16 detailed patterns

From prompts/python.md (current stub to expand):
- Section: "## Common Patterns" (lines 152-158)
- Currently: 4 bullet points with minimal detail
- Structure to preserve: Context, Instructions, Constraints, Before You Start, Fix Workflow, Escalation, Common Patterns, Output

From auto-fix-caller.example.yml:
- 19-line caller template
- Key customization: `workflows: [CI]` list
- Required secrets: ANTHROPIC_API_KEY, AUTO_FIX_APP_PRIVATE_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Python prompt with 6 failure pattern categories</name>
  <files>prompts/python.md</files>
  <action>
Expand the "## Common Patterns" section of `prompts/python.md` (currently lines 152-158) to match the TypeScript prompt's depth and structure. Replace the existing 4 bullet points and the `<!-- TODO -->` comment with 6 detailed categories, each containing 4 sub-patterns. Use the same formatting as `prompts/typescript.md`: bold keyword + colon + multi-sentence guidance.

The 6 categories to add (per user decision from CONTEXT.md):

1. **Import Errors** (4 patterns):
   - ModuleNotFoundError: Check package in requirements.txt/pyproject.toml, verify installed in CI, check typos
   - Circular imports: Trace import chain, break with lazy imports or extract shared types to separate module
   - Relative vs absolute imports: Use absolute (`from app.models import User`) unless project consistently uses relative. Check existing style
   - Missing __init__.py: Verify exists in all package dirs. Some projects use `src/` layout requiring path config in pytest

2. **Missing Dependencies** (4 patterns):
   - Package in code but not in requirements: Add to `requirements.txt` or `pyproject.toml [project.dependencies]`
   - Version conflicts: Check `pip install --dry-run` output for incompatible version constraints between packages
   - Dev-only deps missing in CI: Check if package is in dev group (`[project.optional-dependencies]` or `requirements-dev.txt`) but CI installs only production deps
   - Transitive dependency breakage: A dependency updated and broke its API. Pin the working version or update usage

3. **Fixture Issues** (4 patterns):
   - conftest.py not discovered: Verify conftest.py is in the correct directory relative to tests. pytest discovers conftest.py in the test's parent directories up to rootdir
   - Fixture scope mismatch: `function` scope fixtures recreated per test, `session` scope shared. If test modifies fixture state, use `function` scope
   - Missing fixture dependencies: Fixture A depends on fixture B -- ensure B is available in same or parent conftest.py
   - Autouse fixture side effects: `@pytest.fixture(autouse=True)` runs for ALL tests in scope. Check if side effects interfere with specific tests

4. **Async/Await Bugs** (4 patterns):
   - Missing `await`: Forgetting `await` on async function call returns a coroutine object instead of result. Look for `<coroutine object>` in error output
   - pytest-asyncio mode: Configure `asyncio_mode = "auto"` in `pyproject.toml` or `pytest.ini`. In `strict` mode, every async test needs `@pytest.mark.asyncio`
   - Mixing sync/async: Do not call sync functions with `await` or async functions without `await`. Use `asyncio.to_thread()` for blocking calls in async context
   - Event loop already running: In tests, do not manually create event loops. Use `pytest-asyncio` fixtures (`event_loop`). Avoid `asyncio.run()` inside async tests

5. **Pydantic v2 Migration** (4 patterns):
   - Deprecated validators: Replace `@root_validator` with `@model_validator(mode='before')` or `@model_validator(mode='after')`. Replace `@validator` with `@field_validator`
   - Deprecated methods: Replace `Model.parse_raw(data)` with `Model.model_validate_json(data)`. Replace `Model.parse_obj(data)` with `Model.model_validate(data)`. Replace `.dict()` with `.model_dump()`
   - Field configuration: Replace `Field(..., extra_key=value)` with `Field(..., json_schema_extra={"extra_key": value})`. Replace inner `Config` class with `model_config = ConfigDict(...)`
   - TypeError in validators: In Pydantic v2, `TypeError` raised in validators is NOT converted to `ValidationError`. Validators must catch `TypeError` and raise `ValueError` explicitly if tests expect `ValidationError`

6. **Ruff Lint Patterns** (4 patterns):
   - F401 unused imports: Remove the import or use the imported symbol. For re-exports, add to `__all__` or use `as` alias pattern
   - I001 import sorting: Ruff enforces isort-compatible ordering. Fix: `ruff check --fix --select I .` Auto-fixable.
   - E501 line too long: Break long lines. For strings, use implicit concatenation or parenthesized expressions. Check project's configured line length in `pyproject.toml`
   - N801/N802 naming: Class names should be UpperCamelCase (N801), function names should be lowercase_with_underscores (N802). Follow existing project convention if it intentionally diverges

Also remove the `<!-- TODO: Expand with project-specific patterns in Phase 3 -->` comment since this IS Phase 3.

Do NOT modify any other section of the prompt (Context, Instructions, Constraints, Before You Start, Fix Workflow, Escalation, Output). Only expand Common Patterns.
  </action>
  <verify>
    <automated>grep -c "###" prompts/python.md | xargs test 6 -le && grep -q "Import Errors" prompts/python.md && grep -q "Missing Dependencies" prompts/python.md && grep -q "Fixture Issues" prompts/python.md && grep -q "Async/Await" prompts/python.md && grep -q "Pydantic v2" prompts/python.md && grep -q "Ruff Lint" prompts/python.md && ! grep -q "TODO:" prompts/python.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Python prompt contains 6 named categories with 4 sub-patterns each (~24 patterns total). No TODO comments remain. All other prompt sections unchanged. Pattern depth matches TypeScript prompt.</done>
</task>

<task type="auto">
  <name>Task 2: Create ONBOARDING.md with complete enrollment guide</name>
  <files>ONBOARDING.md</files>
  <action>
Create `ONBOARDING.md` at the repo root with a complete guide for enrolling any new repo into the auto-fix system. Structure:

**# Auto-Fix Agent: Onboarding Guide**

**## What This Does** (1 paragraph)
When CI fails on your repo, the auto-fix agent automatically analyzes the failure logs, implements a source-code-only fix, and opens a PR for human review. It supports TypeScript, Python, and Kotlin stacks. The system uses Claude Code Action with stack-specific prompts and safety guardrails (directory restrictions, diff validation, retry limits).

**## Prerequisites**
- GitHub App (ID: 2985828) installed on your org -- contact repo admin if not installed
- Two secrets configured (see Step 1)
- Your repo listed in `config/repo-stack-map.json` in the central auto-fix-agent repo

**## Step 1: Configure Secrets**
Two secrets are required: `ANTHROPIC_API_KEY` and `AUTO_FIX_APP_PRIVATE_KEY`.

**Org-level secrets (recommended for 2+ repos):**
1. Go to org Settings > Secrets and variables > Actions
2. Add `ANTHROPIC_API_KEY` (from Anthropic dashboard)
3. Add `AUTO_FIX_APP_PRIVATE_KEY` (from GitHub App settings > Private keys)
4. Set repository access policy to "All repositories" or select specific repos

**Repo-level secrets (for single repos or overrides):**
1. Go to repo Settings > Secrets and variables > Actions
2. Add both secrets (same values as above)

Note: Repo-level secrets take precedence over org-level. Cross-org `secrets: inherit` does NOT work -- the caller template passes secrets explicitly.

**## Step 2: Register Your Repo**
Add your repo to `config/repo-stack-map.json` in the central `fbetancourtc/auto-fix-agent` repo:

```json
"your-org/your-repo": { "stack": "python" }
```

If your repo has a non-standard directory structure, add custom `allowed_dirs`:

```json
"your-org/your-repo": {
  "stack": "python",
  "allowed_dirs": ["backend/src/", "api/", "tests/"]
}
```

Stack defaults are defined in the `defaults` section of the same file.

**## Step 3: Deploy Caller Workflow**
1. Copy `.github/workflows/auto-fix-caller.example.yml` from this repo
2. Save as `.github/workflows/auto-fix-caller.yml` in your target repo
3. Customize the `workflows:` list to match your CI workflow name(s):

```yaml
workflows: [CI]  # Must match the 'name:' field in your CI workflow YAML exactly
```

To find your CI workflow name(s):
```bash
grep "^name:" .github/workflows/*.yml
```

4. **Merge to default branch** -- `workflow_run` only triggers from the default branch (main/develop). A PR alone is not sufficient.

**## Step 4: Verify Setup (Smoke Test)**
1. Create a test branch: `git checkout -b test/auto-fix-verify`
2. Introduce a deliberate test failure (e.g., change `assert True` to `assert False` in a test file)
3. Push and wait for CI to fail
4. Wait for the "Auto Fix" workflow to trigger (visible in Actions tab)
5. Verify an auto-fix PR is created with the `auto-fix` label
6. Close the auto-fix PR
7. Delete the test branch (the deliberate failure gets cleaned up)

**## Customization**

**Workflow names:** Edit the `workflows:` list in your caller to match your CI workflows. Use exact names from `name:` fields. Example: `workflows: [CI, Lint, Build]`.

**Directory restrictions:** The agent can only modify files in directories listed in `allowed_dirs`. Stack defaults cover standard layouts. Add per-repo overrides in `repo-stack-map.json` for non-standard structures.

**Stack detection:** The agent selects the fix prompt based on the `stack` field in `repo-stack-map.json`. Supported: `typescript`, `python`, `kotlin`.

**## Troubleshooting**

| Symptom | Cause | Fix |
|---------|-------|-----|
| Auto Fix workflow never triggers | Caller not on default branch OR workflow name mismatch | Merge caller to main/develop. Check `workflows:` matches CI `name:` exactly |
| Workflow fails at "Generate token" | GitHub App not installed on org | Contact org admin to install App ID 2985828 |
| Workflow fails at agent step | Missing ANTHROPIC_API_KEY secret | Add secret at org or repo level |
| Agent modifies wrong files | Missing or incorrect `allowed_dirs` | Update `repo-stack-map.json` with correct dirs |
| PR created against wrong branch | `--base develop` in prompt vs repo default | Verify repo's default branch matches prompt config |
| "needs-human" issue created | Agent failed 2x on same failure | Human review required -- check the linked PRs for what the agent tried |
  </action>
  <verify>
    <automated>test -f ONBOARDING.md && grep -q "Prerequisites" ONBOARDING.md && grep -q "Configure Secrets" ONBOARDING.md && grep -q "Deploy Caller" ONBOARDING.md && grep -q "Verify Setup" ONBOARDING.md && grep -q "Troubleshooting" ONBOARDING.md && grep -q "repo-stack-map" ONBOARDING.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>ONBOARDING.md exists at repo root with all required sections: What This Does, Prerequisites, Configure Secrets, Register Repo, Deploy Caller Workflow, Verify Setup (smoke test), Customization, Troubleshooting. References the caller template and repo-stack-map.json. Provides both org-level and repo-level secret instructions.</done>
</task>

</tasks>

<verification>
1. Python prompt has 6 H3 categories with 4+ sub-patterns each in Common Patterns section
2. No TODO comments remain in python.md
3. Python prompt Context/Instructions/Constraints/Before You Start/Fix Workflow/Escalation/Output sections are unchanged
4. Kotlin prompt is byte-for-byte identical to its pre-plan state (verify with git diff)
5. ONBOARDING.md covers all required sections per CONTEXT.md decisions
6. ONBOARDING.md references auto-fix-caller.example.yml and repo-stack-map.json correctly
</verification>

<success_criteria>
- Python prompt expanded from ~4 patterns to ~24 patterns across 6 categories
- Kotlin prompt unchanged (user decision honored)
- ONBOARDING.md provides self-service enrollment capability
- Both files committed to central auto-fix-agent repo
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-repo-rollout/03-01-SUMMARY.md`
</output>
