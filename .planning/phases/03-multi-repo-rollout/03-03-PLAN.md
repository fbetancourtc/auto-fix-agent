---
phase: 03-multi-repo-rollout
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: true
requirements:
  - ENROLLMENT

must_haves:
  truths:
    - "All 6 Liftitapp repos have caller YAML files prepared and documented for activation"
    - "Activation instructions are clear enough that a 5-minute task enables auto-fix once admin approves GitHub App"
    - "Liftitapp repos are NOT actively enrolled (GitHub App not installed)"
  artifacts:
    - path: ".planning/phases/03-multi-repo-rollout/liftitapp-activation.md"
      provides: "Activation guide for Liftitapp repos when admin approves"
      contains: "liftit-control-de-asistencia|averias-marketplace|geocoding-enterprise"
  key_links:
    - from: ".planning/phases/03-multi-repo-rollout/liftitapp-activation.md"
      to: "ONBOARDING.md"
      via: "References onboarding guide for full setup context"
      pattern: "ONBOARDING"
---

<objective>
Prepare caller workflow files and activation documentation for all 6 Liftitapp repos so they can be activated in a 5-minute task when admin approves the GitHub App installation.

Purpose: The GitHub App (ID: 2985828) is not installed on Liftitapp org -- admin approval is pending. Per user decision, the phase counts as complete without Liftitapp, but caller files should be ready so activation is trivial. This plan audits each repo, prepares the customized caller content, and documents the activation steps.

Output: `liftitapp-activation.md` in the phase directory with per-repo caller configs and activation checklist.
</objective>

<execution_context>
@/Users/fbetncourtc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fbetncourtc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-multi-repo-rollout/03-CONTEXT.md
@.planning/phases/03-multi-repo-rollout/03-RESEARCH.md
@.planning/phases/03-multi-repo-rollout/03-01-SUMMARY.md

@.github/workflows/auto-fix-caller.example.yml
@config/repo-stack-map.json
@ONBOARDING.md

<interfaces>
<!-- Liftitapp repos to prepare (6 repos, GitHub App NOT installed) -->
1. liftit-control-de-asistencia (kotlin) -- has custom allowed_dirs: app/src/, dashboard/src/, backend/src/
2. averias-marketplace (typescript)
3. geocoding-enterprise (typescript)
4. conciliacion-recaudo-liftit (python)
5. liftit-ai-system (typescript)
6. geocoding-liftit-api (python)
7. liftit-cargo-receptor-de-cumplidos (python)

<!-- Caller template (19 lines, only workflows: list changes) -->
See auto-fix-caller.example.yml. Each repo needs the workflows: list customized.

<!-- Activation blockers -->
- GitHub App not installed on Liftitapp org
- No admin access to install it
- Once installed: deploy callers + configure secrets = working auto-fix
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Liftitapp repos and prepare activation documentation</name>
  <files>.planning/phases/03-multi-repo-rollout/liftitapp-activation.md</files>
  <action>
**Step 1: Audit each Liftitapp repo's CI workflow names and directory structure.**

For each of the 6 Liftitapp repos, use GitHub CLI to discover:
- CI workflow name(s): `gh api repos/Liftitapp/{repo}/contents/.github/workflows` then extract `name:` from each workflow file
- Default branch: `gh repo view Liftitapp/{repo} --json defaultBranchRef --jq '.defaultBranchRef.name'`
- Directory structure (for Python repos, check if `src/`, `app/`, `lib/`, `tests/` defaults apply)

Special attention to `liftit-control-de-asistencia` (Kotlin monorepo) -- it already has custom `allowed_dirs` in repo-stack-map.json. Verify the structure still matches.

**Step 2: Create `liftitapp-activation.md` in the phase directory.**

Write a comprehensive activation guide structured as:

```markdown
# Liftitapp Auto-Fix Activation Guide

## Prerequisites
- [ ] GitHub App (ID: 2985828) installed on Liftitapp org by admin
- [ ] Org-level secrets configured: ANTHROPIC_API_KEY, AUTO_FIX_APP_PRIVATE_KEY

## Per-Repo Activation

### 1. liftit-control-de-asistencia (kotlin)
- **CI Workflow name(s):** [discovered name]
- **Default branch:** [main/develop]
- **Custom allowed_dirs:** Yes (already in repo-stack-map.json: app/src/, dashboard/src/, backend/src/)
- **Caller YAML content:**
[Full caller YAML with correct workflows list]
- **Deploy command:**
[gh api command to create the file]

### 2. averias-marketplace (typescript)
[Same structure]

... (repeat for all 6 repos)

## Activation Checklist
- [ ] Admin approves GitHub App installation
- [ ] Configure org-level secrets (ANTHROPIC_API_KEY + AUTO_FIX_APP_PRIVATE_KEY)
- [ ] Deploy caller to each repo (commands above)
- [ ] Smoke test one repo (see ONBOARDING.md Step 4)
- [ ] Verify "Auto Fix" visible in Actions tab of each repo

## Time Estimate
~5 minutes with admin access (copy-paste 6 deploy commands + set 2 org secrets)
```

For each repo, include the EXACT caller YAML content with the discovered CI workflow name, AND the exact `gh api` command to deploy it. This makes activation a copy-paste exercise.

If any repo's CI workflow files are not accessible (private repo, permissions), note "NEEDS MANUAL AUDIT" and provide the fallback: check `.github/workflows/*.yml` for the `name:` field after gaining access.

Also check if any Liftitapp Python repos need custom `allowed_dirs` overrides beyond the stack defaults. If so, note the needed repo-stack-map.json update in the activation guide.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && grep -q "liftit-control-de-asistencia" .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && grep -q "averias-marketplace" .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && grep -q "geocoding-enterprise" .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && grep -q "conciliacion-recaudo-liftit" .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && grep -q "liftit-ai-system" .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && grep -q "geocoding-liftit-api" .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && grep -q "Activation Checklist" .planning/phases/03-multi-repo-rollout/liftitapp-activation.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>liftitapp-activation.md exists with all 6 repos documented, each with discovered CI workflow name, default branch, caller YAML content, deploy command, and any allowed_dirs notes. Activation checklist provides a 5-minute path from admin approval to working auto-fix.</done>
</task>

</tasks>

<verification>
1. liftitapp-activation.md contains entries for all 6 Liftitapp repos (note: research listed 7 but repo-stack-map.json shows the correct 6 repos under Liftitapp org)
2. Each entry includes CI workflow name (discovered or marked for manual audit)
3. Each entry includes the exact caller YAML content
4. Each entry includes the deploy command
5. liftit-control-de-asistencia correctly noted as having custom allowed_dirs
6. Activation checklist is complete and actionable
7. No callers are actually deployed to Liftitapp repos (GitHub App not installed)
</verification>

<success_criteria>
- All 6 Liftitapp repos have prepared caller configs documented
- Activation guide makes enrollment a 5-minute copy-paste task
- No active deployment to Liftitapp (respects GitHub App blocker)
- liftit-control-de-asistencia's custom allowed_dirs acknowledged
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-repo-rollout/03-03-SUMMARY.md`
</output>
