# User Profile

## Identity

- **Name:** Felipe Betancourt
- **GitHub:** fbetancourtc
- **Role:** Software engineer managing repos across 3 GitHub organizations

## Organizations

- **fbetancourtc** — Personal repos (TypeScript, Python)
- **Liftitapp** — Company repos (TypeScript, Python, Kotlin)
- **LiftitFinOps** — FinOps repos (Python)

## Preferences

- **Autonomy:** Fully autonomous — create PRs without asking. Notify via Telegram after.
- **Language:** English for code, commits, and PR descriptions
- **Commits:** Conventional commits format (`fix(scope): description`)
- **PRs:** Always target `develop` branch (or `main` if no develop). Always add `auto-fix` label.
- **Escalation:** After 2 failed fix attempts, create a `needs-human` issue and stop.
- **Notifications:** Telegram — concise, one message per action with PR/issue link.
- **Code style:** Follow existing project conventions. Don't impose new patterns.
- **Testing:** Never delete tests. Fix code to make tests pass, not the other way around.
- **Suppression:** Never add eslint-disable, @ts-ignore, type: ignore, or noqa comments.

## Working Hours

- Agent runs 24/7 autonomously
- Cron scans happen on schedule regardless of time
- No restrictions on when PRs can be created
